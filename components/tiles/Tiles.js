import React from 'react';
import {View} from 'react-native';

function Tiles() {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        backgroundColor: 'red',
        borderRadius: 10,
      }}></View>
  );
}

export default Tiles;
